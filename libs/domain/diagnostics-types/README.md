# diagnostics-types

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build diagnostics-types` to build the library.

## Running unit tests

Run `nx test diagnostics-types` to execute the unit tests via [Vitest](https://vitest.dev/).
