const require_runtime = require("./_virtual/_rolldown/runtime.js");
const require_lockfile = require("./lockfile.js");
let node_fs = require("node:fs");
node_fs = require_runtime.__toESM(node_fs);
//#region src/readFile.ts
/**
*  Reads a local file, which involves
*  optionally taking a lock
*  reading the file contents
*  optionally parsing JSON contents
* @param {*} name
* @param {*} options
* @param {*} callback
*/
function readFile(name, options = {}, callback = () => {}) {
	if (typeof options === "function") {
		callback = options;
		options = {};
	}
	options.lock = options.lock || false;
	options.parse = options.parse || false;
	const lock = function(options) {
		return new Promise((resolve, reject) => {
			if (!options.lock) return resolve(null);
			require_lockfile.lockFile(name, function(err) {
				if (err) return reject(err);
				return resolve(null);
			});
		});
	};
	const read = function() {
		return new Promise((resolve, reject) => {
			node_fs.default.readFile(name, "utf8", function(err, contents) {
				if (err) return reject(err);
				resolve(contents);
			});
		});
	};
	const parseJSON = function(contents) {
		return new Promise((resolve, reject) => {
			if (!options.parse) return resolve(contents);
			try {
				contents = JSON.parse(contents);
				return resolve(contents);
			} catch (err) {
				return reject(err);
			}
		});
	};
	Promise.resolve().then(() => lock(options)).then(() => read()).then((content) => parseJSON(content)).then((result) => callback(null, result), (err) => callback(err));
}
//#endregion
exports.readFile = readFile;

//# sourceMappingURL=readFile.js.map