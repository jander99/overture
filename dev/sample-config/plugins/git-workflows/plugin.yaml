name: git-workflows
version: 1.0.0
description: Git workflow automation including commit hooks and PR management
author: overture-team
repository: https://github.com/overture-plugins/git-workflows

# Features provided by this plugin
provides:
  hooks:
    - pre-push-test
    - commit-message-template
  commands:
    - create-pr
    - sync-branch

# Dependencies on other plugins
dependencies:
  plugins: []

# Metadata
tags:
  - git
  - workflow
  - automation
  - ci-cd
license: MIT
