# skill

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build skill` to build the library.

## Running unit tests

Run `nx test skill` to execute the unit tests via [Vitest](https://vitest.dev/).
