# Configuration with Unicode characters
version: "2.0"

mcp:
  custom-server:
    command: npx
    args: ["-y", "server-with-unicode"]
    env:
      MESSAGE: "Hello 世界 🌍"
      PATH_WITH_UNICODE: "/home/user/フォルダ/data"
    transport: stdio
    metadata:
      description: "Server supporting internationalization"
      tags: ["i18n", "unicode", "国際化"]
