# client-adapters

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build client-adapters` to build the library.

## Running unit tests

Run `nx test client-adapters` to execute the unit tests via [Vitest](https://vitest.dev/).
