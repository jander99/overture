# Overture Configuration # This file defines plugins and MCP servers for your
project. # Generated by: overture init # Configuration version version: "1.0" #
Project information project: # Project name name:
{{projectName}}

# Project type (e.g., python-backend, typescript-web, java-api, etc.)
{{#if projectType}}
  type:
  {{projectType}}
{{else}}
  type: general
{{/if}}

# Project description (optional)
{{#if projectDescription}}
  description:
  {{projectDescription}}
{{else}}
  # description: "A brief description of your project"
{{/if}}

# Claude Code plugins to install # Each plugin can declare recommended MCP
servers plugins: # Example: # python-development: # marketplace:
claude-code-workflows # enabled: true # mcps: [python-repl, ruff, filesystem] {}
# MCP server configurations # MCPs defined here are project-scoped (synced to
.mcp.json) # Global MCPs should be defined in ~/.config/overture.yml mcp: #
Example project-scoped MCP: # python-repl: # command: uvx # args:
[mcp-server-python-repl] # transport: stdio # enabled: true # # Note: Scope is
implicit based on file location # - MCPs here (.overture/config.yaml) are
project-scoped # - MCPs in ~/.config/overture.yml are global {}