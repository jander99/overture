const require_runtime = require("./_virtual/_rolldown/runtime.js");
let lockfile = require("lockfile");
lockfile = require_runtime.__toESM(lockfile);
//#region src/unclock.ts
function unlockFile(name, next) {
	const lockFileName = `${name}.lock`;
	lockfile.default.unlock(lockFileName, function() {
		return next(null);
	});
}
//#endregion
exports.unlockFile = unlockFile;

//# sourceMappingURL=unclock.js.map