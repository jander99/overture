# output

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build output` to build the library.

## Running unit tests

Run `nx test output` to execute the unit tests via [Vitest](https://vitest.dev/).
